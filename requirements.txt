# Блок 0 — Фаза А (на заглушке). Минимальный набор.
fastapi          # вебхук + быстрый ack
uvicorn[standard]  # сервер для запуска FastAPI
redis            # клиент Redis (память/дедуп)
fakeredis        # Redis "понарошку" для тестов — без отдельного сервиса
httpx            # HTTP-клиент (к Fitbase позже) + используется в тестах
pytest           # поведенческие тесты = приёмочный чек-лист
