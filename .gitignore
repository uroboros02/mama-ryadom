# Секреты — никогда не в git
.env
.env.*
!.env.example
*.key
*.pem

# Python (генерируется само, в git не нужно)
__pycache__/
*.pyc
.venv/
venv/

# Персональные данные клиентов — НЕ коммитим (152-ФЗ, есть GitHub-remote)
chats wazzup.csv
*.csv
data/

# Мусор и локальные индексы
.DS_Store
.codegraph/
