# Мама рядом (Mama Ryadom) — AI-агент продаж (архитектура v2)

> **План работы:** архитектура и дорожная карта готовы. **Дизайн F1+F2 (на бумаге) закрыт** —
> [`docs/f1-f2-react-qualify-faq.md`](docs/f1-f2-react-qualify-faq.md) (шаги 1–8). ✅ **Блок 0
> (платформа) СОБРАН — Фаза А (на заглушке), шаги 0–8 закрыты** (`app/`, 59 поведенческих тестов,
> приёмочный чек-лист `block-0.md` зелёный на mock+fakeredis). Дизайн — [`docs/block-0.md`](docs/block-0.md),
> ход сборки — [`docs/block-0-build-plan.md`](docs/block-0-build-plan.md). Дальше: **F1** (мозг) и
> **Фаза Б** — живой триал Fitbase/Wazzup; упираемся во внешнее (доступы/ключи, 152-ФЗ) —
> [`docs/tasks-open.md`](docs/tasks-open.md).

> **Как мы работаем:** принципы разработки, простоты и безопасности — в
> [`PRINCIPLES.md`](PRINCIPLES.md). Сверяться перед стартом блоков.

## Контекст

Строим автономного AI-агента продаж для детского центра «Мама рядом» (Mama Ryadom), Уфа.
Бот ведёт клиента от заявки до зачисления, **обрамляя КЭВ** (см. ниже): общается в
мессенджерах, помнит контекст между сессиями, ведёт карточку в CRM, ставит задачи,
бронирует/перезаписывает пробные, анализирует записи звонков продавца.

- **Ниша:** B2C, детский центр, дети **1,5–4 года** (по нескольким критериям; **утв. заказчиком 2026-06-02**). Три направления —
  **гимнастика**, **беби балет**, **сенсорика**; детали в разделе «Продукты и возрастные критерии». Цикл сделки длинный и рваный (заявка → дозвон со
  2–3 раза → молчит днями → просит позвонить → ушёл подумать → записался → не пришёл →
  перезапись → уехал на 2 недели → и только потом купил). Это закладываем в память и задачи.
- **Команда:** один продавец. Маршрутизация/распределение заявок не нужны.
- **Масштаб:** пока один бизнес («Мама рядом»). Мультитенант не закладываем.
- **Backend/CRM:** **Fitbase** (`https://api.fitbase.io/api/v2`). Не Firebase — это была
  опечатка. Старая CRM `lk.ufagymnastics.ru` выводится из эксплуатации (см.
  `old-bot-architecture.md` как легаси-референс).
  - 🔴 **ПЕРЕСМОТР CRM (2026-06-23):** бронь через API Fitbase **невозможна** (вендор подтвердил) →
    Fitbase отпал для автономии. Выбираем замену (**Мой Класс** / **1С:Фитнес клуб**) —
    [`docs/crm-evaluation.md`](docs/crm-evaluation.md). До выбора репозиторий ниже ещё описывает Fitbase.

**Подход к автономии:** цель — полная автономия, но строим поэтапно через
**human-in-the-loop**: бот действует/предлагает, продавец может вмешаться. «Страховочные
колёса» снимаем по мере роста доверия. Каркас под автономию ≈ +20% к версии «помощник»
(не «с нуля автономного»). Полную автономию НЕ делаем условием запуска.

---

## Роль бота — «обрамляет КЭВ»

**КЭВ** (ключевой этап воронки) = **пробная тренировка с презентацией** — момент, где
продаётся основной продукт (абонемент).

**Пробная — средство, не обязательный барьер.** Цель — продать абонемент; пробная лишь
помогает. Готов лид оформить абонемент **сразу, без пробной** — бот продаёт сразу, не гонит
через пробную. Редко, но бот не должен «заставлять» проходить пробное.

- **До КЭВа — целиком бот:** реакция на заявку ≤5 мин, квалификация, «продать» пробную и
  записать. Здесь главная зона автономии.
- **КЭВ — оффлайн, человек:** саму пробную ведёт тренер/продавец. Бот не внутри.
- **После КЭВа — снова бот:** подтверждение прихода, догрев «пришёл → думает → зачислен»,
  обработка неприхода и перезаписи.

---

## Зона бота — только продажи (лиды), не действующие клиенты

Бот работает **только с лидами / потенциальными клиентами**. С действующими (ходящими)
клиентами он не общается и не выполняет по ним действий — это отдельная база, **зона админа**.

**Роли (для ясности терминологии):** действующие (платящие, ходящие) клиенты — забота
**админа**; заявки и те, кто ещё не оплатил абонемент — забота **продавца**. Бот **занимает
роль продавца** (лиды / неоплатившие).

- На входе в диалог бот **проверяет надёжные идентификаторы** (телефон / id канала: MAX, VK,
  TG) по базе действующих клиентов Fitbase (напр. активный `ClientContract`).
- Совпало по надёжному ключу → действующий клиент: чат уходит **админу**, бот **отключается**
  в этом чате (не тратит ресурсы). **По имени НЕ сверяем** (решение 2026-05-29): тёзок слишком
  много («куча Гузелей»/Елен), ложное отключение лида дороже редкого случая «клиент написал с
  нового номера» — такого бот спокойно ведёт как лида, без тихих заметок админу.
- Когда лид **становится** клиентом (купил абонемент): на подтверждённой оплате — **handoff
  продавцу** (задача+алерт «проверь оплату, дальше веди сам»). Это граница лид→клиент. Продажу
  (дожим до покупки) ведёт бот (F6); пост-продажный онбординг (связать с тренером, «теперь
  вопросы сюда») = **F7**, пока вручную человеком.
- **Бывший клиент вернулся** (абонемент кончился) — снова **лид** (бот прогревает), но
  карточка/память помнят, что уже ходили. Кейсов мало.

---

## Сквозные инварианты (соблюдает любой блок)

Правила безопасности и поведения, не зависящие от блока. Детали — в `docs/`, отслеживание
внедрения — в [`docs/tasks-open.md`](docs/tasks-open.md).

- **Один человек = один лид** — найти карточку до создания, не плодить дубли. Имя — только слабая подсказка, не ключ автосклейки.
- **Fitbase = источник правды**, кэш — только запаска (необратимое/деньги на кэше не делаем).
- **Бот говорит с F1** — в Блоке 0 клиенту не пишет.
- **Память за человеком, не за каналом.**
- **Зона бота — только лиды** (см. выше): действующий клиент → бот off, к админу; по имени не сверяем.
- **Бот сам выходит на контакт** (каскад MAX→WhatsApp→задача; задача продавцу — последний шаг, не дефолт).
- **Турникет Fitbase** ≤20 rps + бэк-офф на 429; **идемпотентность** по `messageId`.
- 🔴 **Claude только предлагает — код проверяет и исполняет** любые действия с деньгами/скидками/этапами/бронью (LLM не дёргает кнопки напрямую).
- 🔴 **ПД маскируем перед отправкой в LLM:** телефон/фамилия в Claude не уходят; имена для общения — ок. Опознание «кто это» — в нашем коде.
- 🔴 **Стоп-кран всегда:** эхо-фильтр (не реагировать на свои и продавца сообщения) + проверка перед каждой отправкой + автостоп по счётчику. Бот не должен «строчить сам с собой».
- 🔴 **Единый часовой пояс — Уфа (UTC+5)** для всех дедлайнов/напоминаний/тихих часов.

---

## Архитектура — 3 части

```
┌─────────────────────────────────────────────┐
│              КАНАЛЫ (транспорт)              │
│   MAX 60% · WhatsApp 30% · TG 5% · VK 5%     │
│        Wazzup (агрегатор)  ·  Звонок         │
└───────────────────┬─────────────────────────┘
                    │ входящее (push из канала)
┌───────────────────▼─────────────────────────┐
│         РОУТЕР + ИИ-АГЕНТ  (наш слой)        │
│  · идентификация лида по каналу, дедуп        │
│  · LLM (Claude) + инструменты                 │
│  · память: кратко / долго / эпизод + ретривал │
│  · планировщик проактива, опрос Fitbase       │
│  · решает: ответить сам или эскалировать      │
└──────┬───────────────────────────┬───────────┘
       │ читать / писать            │ задача + алерт
┌──────▼──────────────┐   ┌─────────▼───────────┐
│  FITBASE (CRM,       │   │   ПРОДАВЕЦ (1 чел.) │
│  источник правды)    │   │  Fitbase Task +     │
│  лид, воронка,       │   │  Telegram-алерт     │
│  оплаты, брони,      │   └─────────────────────┘
│  посещения, задачи   │
└──────────────────────┘
```

---

## Стек

- **Python**; **LLM Claude** (Anthropic API, tool use, prompt caching) — рабочая модель диалога **Sonnet** (Opus не нужен; Haiku для тривиальных под-шагов — на будущее).
- **Redis** — история диалога / состояние агента / дедуп.
- **Wazzup** — транспорт MAX/WhatsApp (и TG/VK при необходимости).
- **Fitbase** — CRM / источник правды.
- **Мегафон-АТС** — записи звонков (Фаза 2).

---

## Карта документации (детали вынесены в docs/)

Чтобы держать этот файл лёгким, развёрнутые разделы вынесены в `docs/` **дословными
копиями**. Открывай нужный файл, когда садишься за соответствующий блок.

- [`docs/channels.md`](docs/channels.md) — **Каналы (транспорт):** таблица каналов, доли трафика, мультиканальная дедупликация, VK как две двери.
- [`docs/fitbase-and-inbound.md`](docs/fitbase-and-inbound.md) — **Что где живёт** (сущности Fitbase, опрос/polling, лимит 20 rps) · **Две двери входящего** (A/B, каскад касаний) · **Воронка и этапы**.
- [`docs/crm-evaluation.md`](docs/crm-evaluation.md) — 🔴 **ВЫБОР CRM (Fitbase → ?) — ИСТОЧНИК ПРАВДЫ по выбору CRM:** Fitbase отпал (бронь по API невозможна, вендор подтвердил 2026-06-23); разбор **Мой Класс** vs **1С:Фитнес клуб** vs MobiFitness, затыки, открытые вопросы, черновики вопросов вендорам, следующие шаги.
- [`docs/fitbase-integration.md`](docs/fitbase-integration.md) — ⚠️ *(Fitbase в пересмотре — см. `crm-evaluation.md`)* **Fitbase — контракт интеграции (API v2):** авторизация (token+domain), эндпоинты, как наша модель ложится на CRM, 🔴 ограничения (бронь через API **подтверждено невозможна**, не «must-solve»), опрос-радар, чек-листы.
- [`docs/payments-acquiring.md`](docs/payments-acquiring.md) — **Эквайринг и разноска оплат (Альфа + бот):** корень = реконсиляция платежей (Fitbase нативно дружит только с CloudPayments/Payselection/TipTop, в API нельзя записать оплату). Инсайт: разнести «детектив кто-за-что» и «механику посадки». **Альфа интернет-эквайринг даёт callback-вебхук** (проверено 2026-06-23) → бот видит оплату мгновенно с нашим `orderNumber`. 🔴 Открытое: какой продукт Альфы реально стоит (интернет vs торговый); автопосадка в ЛК Fitbase всё равно не решается → довод за Мой Класс.
- [`docs/products-memory-payment.md`](docs/products-memory-payment.md) — **Продукты и возрастные критерии** · **Память клиента** (кратко/долго/эпизод, активный канал, чтение карточки) · **Пробное и оплата** (прайс, бронь, скидки).
- [`docs/escalation.md`](docs/escalation.md) — **Эскалация / handoff** (три режима, таблица триггеров, ручной режим) · **Проактивные касания** · **Звонки (Фаза 2)** · **Guardrails / идемпотентность / аудит**.
- [`docs/pipelines-roadmap.md`](docs/pipelines-roadmap.md) — **Тех-пайплайны** · **Инструменты агента (tools)** · **Дорожная карта (блоки F1–F9, волны)**.
- [`docs/block-0.md`](docs/block-0.md) — **Блок 0 — сводно** (схема на одном экране) · **Блок 0 — компоненты (детально)** + чек-лист «что добыть/подтвердить».
- [`docs/block-0-build-plan.md`](docs/block-0-build-plan.md) — **План сборки Блока 0 (Фаза А, на заглушке):** что значит mock-first · стек · раскладка файлов · шаги 0–8 (каждый закрывает группу приёмочного чек-листа). «Как строим» — производное от `block-0.md` («что строим»).
- [`docs/f1-f2-react-qualify-faq.md`](docs/f1-f2-react-qualify-faq.md) — **F1+F2 (дизайн готов, шаги 1–8):** границы · поток (схема) · логика квалификации · промпт v2 · маскирование · эскалация · инструменты/tools · **приёмочный чек-лист (Шаг 8)**.
- [`docs/config.md`](docs/config.md) — **Конфиг бота (фактбук):** направления/возраст/края · уровни · вместимость · этапы воронки · прайс · скидки · расписание (тест) · адрес · памятка · персона (Элина). Данные, правятся без кода; бот их озвучивает, FAQ пишет сам.
- [`docs/unit-economics-olimpico.md`](docs/unit-economics-olimpico.md) — **Юнит-экономика действующего центра (факты из выгрузок, июль 2026):** LTV ~11 тыс / медиана 6 тыс · продление по когортам 22%→94% · воронка сырой лид→оплата (VK 13%, сайт 27%) · CPL факт 780 ₽ · плотность платежей 59% · сезонность · предпродажи (единый 74%). Вводные финмодели + границы экономики для бота.
- [`docs/brand-core.md`](docs/brand-core.md) — **Ядро бренда (черновик):** позиционирование («место для мам, которые ценят себя»), ценности, стандарт сервиса, продукт/модель, цена, открытые вопросы. Выжимка из 8 аудио-сессий (`records/transcripts/`); сырьё для бренд-бука. Проработка — задача «Бренд / бренд-бук» в `tasks-open.md`.
- [`docs/brand-method.md`](docs/brand-method.md) — **Метод бренд-платформы:** как собираем бренд (onlyness Ньюмейера = стержень + архетип «Заботливый» для голоса + пирамида как чек-лист); скелет платформы, правила работы, источники для чтения. «Как делаем» для бренда (содержание — в `brand-core.md`).
- [`docs/brand-platform.md`](docs/brand-platform.md) — **Бренд-платформа (СОБРАНА, §1–§7):** стержень «где о маме заботятся не меньше, чем о ребёнке» · отличие · ценности · личность/тон · опыт клиента · имя+слоган+визуал · **PDF для людей**. Готовый PDF + генератор — [`docs/brand-pdf/`](docs/brand-pdf/) (Chrome headless, без зависимостей). Визуал — брендбук от дизайнера в [`docs/brand-assets/`](docs/brand-assets/) (палитра/шрифты/лого). Метод — `brand-method.md`, сырьё — `brand-core.md` + опросник-знакомство (CSV в `docs/`). Открытое: согласовать смысл имени с дизайнером (см. `tasks-open.md`).
- [`docs/tasks-open.md`](docs/tasks-open.md) — **Задачи проекта по блокам** (Блок 0 / F1 / F3 / … + ПД-безопасность, организационное) · доработки из ревью · **Открытые вопросы**.
- [`PRINCIPLES.md`](PRINCIPLES.md) — принципы разработки, простоты и безопасности.

### Как добавлять и поддерживать документацию (конвенция проекта)

- **`CLAUDE.md` держим лёгким** — только ядро (оно грузится в каждую сессию). Всё развёрнутое живёт в `docs/`.
- **Новый блок (напр. F1)** → заводим отдельный файл `docs/f1-….md`, складываем туда детали и **добавляем ссылку в список выше**. Так проект растёт, не раздувая `CLAUDE.md`.
- ⚠️ **Ссылки — только обычным markdown** `[текст](путь)`. **Не** писать `@путь`: знак `@` = авто-загрузка файла в контекст при старте сессии (вернёт раздувание до прежних размеров). Обычная ссылка читается **по требованию**, когда реально нужна.
- **Перенос текста между файлами — дословный копипаст** (надёжнее через извлечение строк, не перепечатывание): ничего не теряем; смысловые правки — отдельным осознанным шагом.
